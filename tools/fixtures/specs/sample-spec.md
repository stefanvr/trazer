# Sample — Domain specification

Fixture for `spec-coverage.test.mjs`. Not a real specification.

- **[DS-1.1]** A rule that is both implemented and tested.
- **[DS-1.2]** A rule that is implemented but never tested — the case the separate reporting exists
  to make visible.
- **[DS-1.3]** A rule that is neither.

A cross-reference to [DS-1.1] in ordinary prose, unbolded. This is a mention, not a declaration, and
must not create a fourth identifier.

> ### ⚠️ Illustrative example — delete this whole block
>
> - **[DS-8.8]** An example rule inside a blockquote. It must never be counted, or every template
>   ships with its own worked examples reported as uncovered rules.
