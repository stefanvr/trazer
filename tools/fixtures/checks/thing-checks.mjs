// Fixture "test" file, read as text by the tool rather than executed. Deliberately not named
// *.test.mjs and not placed in a directory called test, so the real test runner does not collect it.

export const cases = [
  "[DS-1.1] the rule holds when the obvious thing is done",
  "[DS-9.9] cites a rule that no longer exists — this is the dead citation",
];
