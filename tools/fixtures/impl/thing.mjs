// Fixture module. Implements [DS-1.1] and [DS-1.2] — the opening comment naming the rules it
// implements is the convention design-guide.md requires, and is what the implementation scan reads.

export const thing = 1;
